# Backend ConfigMap (Non-sensitive environment variables)
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: zero-to-running-app
  labels:
    app: backend
    tier: backend
data:
  NODE_ENV: "production"
  API_PORT: "8080"
  # Database and Redis URLs will come from secrets
  # (auto-generated by env-to-k8s-secrets.sh)

